# Calculating
calculation = 154 / 78
rounded = round(calculation)
int = trunc(calculation)


# Printing output
IO.puts("154 / 78")
IO.puts("Raw version: #{calculation}")
IO.puts("Rounded: #{rounded}")
IO.puts("Integer part: #{int}")
